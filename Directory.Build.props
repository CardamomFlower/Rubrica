<Project>

  <!--
    Read by every project under this folder. The version lives here and nowhere else:
    Rubrica.exe carries it, and so does the installer, which writes it into Settings > Apps.
  -->
  <PropertyGroup>
    <Version>0.2.0</Version>
  </PropertyGroup>

</Project>
